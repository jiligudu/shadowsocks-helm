apiVersion: v1
kind: Service
metadata:
  name: {{ template "shadowsocks.fullname" . }}
  labels:
    app: {{ template "shadowsocks.name" . }}
    chart: {{ template "shadowsocks.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.shadowsocksPort }}
      targetPort: 80
      protocol: TCP
      name: shadowsocks
  selector:
    app: {{ template "shadowsocks.name" . }}
    release: {{ .Release.Name }}
